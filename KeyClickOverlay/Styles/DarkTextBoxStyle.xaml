<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Brushes for Dark Theme -->
    <SolidColorBrush x:Key="SelectionColor" Color="#5E9BFF" />
    <SolidColorBrush x:Key="MainColor" Color="#2B2B2B" />
    <SolidColorBrush x:Key="AccentColor" Color="#414141" />
    <SolidColorBrush x:Key="DarkerAccentColor" Color="#414141" />
    <SolidColorBrush x:Key="BrighterAccentColor" Color="#686868" />
    <SolidColorBrush x:Key="AlmostLightModeAccentColor" Color="#292929" />

    <Style TargetType="TextBox">
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="Snow" />
        <Setter Property="SelectionBrush" Value="{StaticResource SelectionColor}" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="3.5">

                        <ScrollViewer x:Name="PART_ContentHost" />

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <!-- Idle -->
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="False">
                <Setter Property="Background" Value="{StaticResource AccentColor}" />
                <Setter Property="BorderBrush" Value="{StaticResource BrighterAccentColor}" />
            </Trigger>

            <!-- Hover -->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource DarkerAccentColor}" />
                <Setter Property="BorderBrush" Value="{StaticResource AlmostLightModeAccentColor}" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>